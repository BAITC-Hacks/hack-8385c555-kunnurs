# Использование Codex

## 23.09.2026 — русский, казахский и английский интерфейс

По прямому запросу пользователя Codex добавил в шапку переключатель RU / ҚАЗ / EN,
общий React-контекст языка и словарь интерфейса. Выбор сохраняется в localStorage;
обновляются язык документа и заголовок вкладки. Переводятся формы, навигация,
названия 14 мер, районов, показателей, ограничения и сравнение. Перевод применяется
к React-тексту при отображении, без изменения каталога и DOM вне React.
Переключение сохраняет текущий набор и не отправляет новый расчёт.

В соответствии с прежним требованием серверные тексты AI, notice и tradeoffs
выводятся дословно на русском; для других языков показано пояснение. Расчёт,
серверный валидатор, API и промпты не менялись. Новых зависимостей нет.

Проверены 78 основных Python-тестов, 80 OPS, 32 fallback, 10 frontend-тестов и
production-сборка через scripts/check.py --require-frontend. Конечный тест Edge
подтвердил смену трёх языков, отсутствие русского UI-текста в английском режиме
вне явно обозначенных серверных текстов, неизменность выбранных мер и AI-разбора,
отсутствие нового расчёта при смене языка, клавиатуру, сохранение языка после
перезагрузки и казахское сообщение валидации. Осмотрен мобильный скриншот 390 px.
Проверка использовала FastAPI TestClient с mock и пустым ключом: серверы и живой
провайдер агент не запускал. Два промежуточных браузерных прогона остановились
на некорректной отправке Enter в тестовом CDP-клиенте; после передачи символа
нажатия проверка прошла. Существующее предупреждение Starlette/HTTPX остаётся.

## 23.09.2026 — первичная архитектура

Запрос пользователя: прочитать два PDF с ТЗ и датасетом, сразу создать архитектуру и отправить в GitHub. Пользователь ранее разрешил LEAD самостоятельно коммитить и пушить.

Codex извлёк весь текст 3 страниц задания и 5 страниц датасета через pypdf, перенёс численные правила, создал спецификацию, архитектуру, контракты, backend, mock AI, стартовый frontend, скрипты проверок и задачи команды. Исходные PDF не добавлялись в репозиторий. Внешние шаблоны приложения не копировались.

Принятое решение: подробные правила датасета имеют приоритет над неоднозначным требованием «5 решений по направлениям». Разрешены минимум 3 направления при максимуме 2 мер на направление; пример организаторов этому соответствует. Вся численная модель отделена от AI.

Подтверждены базовый Score 52.55768 и пример стоимостью 95 со Score 56.54307. Контроль включает ограничения бюджета/направлений/повторов/районов, все типы конфликтов, лаги, синергии, отрицательный эффект, clip, порог строго <40, все 120 перестановок примера и независимость запросов. 32 автоматические проверки прошли; React/Vite production-сборка прошла. Серверы и браузерное демо не запускались.

Ограничения: реальная LLM, e2e браузера, публичный деплой и база данных ещё не реализованы. При AI_MODE=live каркас честно возвращает fallback. Текущая версия — основа для работы команды, не завершённый продукт хакатона.

## 23.09.2026 — живой AI-анализ

По запросу «подключай и делай дальше» Codex реализовал OpenAI Responses API через SDK 2.54.0 и Structured Outputs. Использован навык OpenAI Docs; схема вызова, разбор структурированного результата и ограничения повторов сверены с официальной документацией. Числовая сводка остаётся серверной; модель формирует качественные объяснения и гипотезы для сравнения.

Добавлены общий deadline в пределах таймаута frontend, максимум один повтор SDK, fallback с фиксированной причиной, маскирование текстовых ПДн, кэш успешных ответов на час/128 записей, конечный smoke-test без запуска серверов. Формула, каталог и зоны FRONTEND/OPS не менялись. Необязательные поля source/reason документированы, примеры обновлены. Инструкция активации находится в ai/README.md; OPS передана задача обновить описания в своих документах.

Пользователь добавил ключ в локальный .env; значение не выводилось и в Git не включалось. Первый запрос с таймаутом попытки 6 секунд вернул fallback/timeout. Небиллинговая проверка доступности модели прошла; после изменения таймаута попытки на 12 секунд при неизменном общем deadline 12 и сокращения ответа реальный запрос завершился успешно: mode=live, source=provider, reason=null, score=56.54307. Вызов скрипта занял около 7 секунд.

Проверка: 53 теста, включая реальный SDK с подменённым HTTP-транспортом, успешный и некорректный ответ, отказ, обрыв, неверный ключ, квоты, сетевой сбой, повтор, отмену по deadline, кэш и отсутствие ключей/сырых ошибок в логах. scripts/check.sh --require-frontend завершился успешно, production-сборка React прошла. Есть предупреждение Starlette об устаревании HTTPX-интеграции TestClient; на результат проверок не влияет.

Для UI нужен перезапуск уже работающего backend, затем новый расчёт. Серверы агент не запускал/не останавливал. Публичный деплой, браузерные e2e и сохранение сценариев остаются следующими задачами.

## 23.09.2026 — проверяемые факты и конкретные рекомендации

Повод: пользователь показал живой ответ с ошибочным утверждением о «трёх мерах социальной политики». В исходном наборе две социальные меры, но три меры с лагом три квартала. Запрос: доработать дальше.

Codex разделил группировку по направлениям и лагам и заменил свободный вывод модели на выбор ID готовых фактов. Structured Outputs сами по себе не гарантируют смысловую точность; с учётом официального руководства OpenAI Docs выбран более узкий формат AISelection. Модель ранжирует факты и советы, сервер проверяет принадлежность ID соответствующим категориям и сам формирует отображаемые утверждения. Неизвестные ID, дубли и свободный текст отклоняются. Версия промпта/кэша изменена на city-evidence-selection-v2.

Добавлен детерминированный перебор всех допустимых замен одного решения. До трёх лучших улучшений включаются в новый необязательный массив alternatives; каждое содержит полный набор для повторного расчёта, Score, бюджет и ухудшения показателей. Советы не применяются автоматически и не объединяются между собой. При fallback эти рассчитанные варианты также доступны. Движок формулы, исходный датасет и чужие зоны не менялись.

Проверка: 66 тестов и production-сборка frontend прошли через scripts/check.sh --require-frontend. Тесты включают наблюдавшуюся ошибку направления/лага, недопустимые ID и категории, бюджет/правила каждой альтернативы, повторный расчёт её Score, независимость порядка и отсутствие заявления о глобальном оптимуме.

Живой запрос с локальным ключом завершился mode=live, source=provider примерно за 4.7 секунды. В тексте правильно указаны соцсфера — 2 и отдельная группа из 3 мер с лагом 3 квартала. AI выбрал замену M5 в Сарыарке на M3 в Нуре: стоимость 100, Score 57.20556 против 56.54307. Компромиссы явно указаны: качество воздуха Сарыарки −8.75 и надёжность ЖКХ −2.50 относительно исходного выбранного набора. Ключ не выводился и не добавлялся в Git. Запуска серверов не было.

## 23.09.2026 — интеграция двух участников

По запросу пользователя получены свежие удалённые ветки и без конфликтов объединены FRONTEND 806e612/3110e39 и OPS dce6529 с main e5776fb, с сохранением авторства. Вошли индикатор режима AI, генератор/демонстрационные сценарии, API/security/offline тесты, упаковка Docker и эксплуатационные документы. Численная модель и локальные секреты не изменялись.

Codex добавил OPS mock/fallback в общую команду scripts/check.py. Дочерние процессы получают пустой OPENAI_API_KEY, который также предотвращает подстановку личного ключа через dotenv; в этой задаче реальных запросов LLM не было. Проверены 66 основных тестов, 79 OPS mock/offline/security и 31 повторный API-тест fallback; production-сборка frontend прошла. Один повтор общего прогона обнаружил недоступную временную папку pytest в Windows; повтор с отдельной созданной временной директорией прошёл. Предупреждение HTTPX/Starlette остаётся неблокирующим.

Обнаружены интеграционные долги в зоне OPS: dockerignore исключает runtime-модуль ai/cache, прямой fallback-прогон ещё считает live заглушкой, документация описывает устаревшую реализацию. Зафиксированы в TASKS.md; чужие файлы не переписывались, разрешение на точечный ремонт запрошено отдельно. Docker Engine локально не работает; объединённый контейнер, браузерное демо и публичный URL в этой задаче не проверялись. Старые отчёты OPS относятся к их версии и не заменяют проверку текущего main. Серверы не запускались.

## 23.09.2026 — обязательные риски, leave-one-out и применение замен

Запрос пользователя: получить исправления Docker от тиммейта; всегда показывать все критические значения и остаток бюджета >10, привязать сильные стороны к мерам методом leave-one-out, отмечать распределение в благополучные районы при критических значениях в других, предложить замены по трём критериям и кнопки применения. Получен и объединён OPS e9cf518 (исправление упаковки ai/cache, безопасные offline тесты и обновление live-документации). Авторство сохранено, OPS-файлы самостоятельно не редактировались. Frontend доработан по прямому запросу пользователя, исключение из разделения зон зафиксировано в TASKS.

OpenAI Docs и официальное руководство Structured Outputs подтвердили, что схема не гарантирует смысловую полноту. Поэтому сервер принудительно добавляет все mandatory_risk_ids во всех режимах и показывает все проверенные рекомендации, а модель выделяет сильные стороны и дополнительные риски. Версия кэша изменена на city-evidence-selection-v3-loo. Численная модель и валидатор пяти мер не менялись: расчёт без одной меры — внутренний диагностический вызов с полным пересчётом синергий, clip и штрафов; его вклады не складываются.

Из полного перебора улучшающих замен выбираются лучший Score, минимальная полная стоимость и минимум критических значений. Совпавшие победители объединяются, а не выдаются за разные варианты. Для пользовательского набора (стоимость 81, Score 54.008819): M9 Есиль → M7 Нура даёт Score 55.30822, стоимость 95 и один критический показатель; это одновременно лучший Score и минимум критических. Самый дешёвый вариант — M5 Сарыарка → M11 Нура, стоимость 66, Score 54.156309, два критических. Все потери показателей показаны. В предупреждениях явно указаны оба провала Нуры, остаток 19 и компромиссы расходов в районах без исходных значений <40; это не объявляется отсутствием пользы от мер.

Кнопка «Применить» проверяет актуальность совета, сохраняет порядок формы, подставляет одну замену и отправляет /analyze. Добавлены блокировка двойного клика, сброс устаревших рекомендаций при изменении формы и возможность повтора после ошибки. Серверные схемы/примеры/API/SPEC и инструкции синхронизированы.

Проверки: 78 основных тестов, 80 OPS mock/offline/security, 32 fallback и 4 frontend unit-теста; production-сборка успешна. Тесты с настоящим SDK и mock-транспортом проверяют, что live/cache не скрывают обязательные риски даже при их пропуске моделью; проверены 50 критических значений, пороги 40 и остатка 10/11, отрицательный вклад, синергии и все критерии выбора замен. Отдельный конечный Chromium-тест загрузил production assets через CDP, использовал FastAPI TestClient с отключённым ключом и проверил обе карточки, форму/Score после применения, двойной клик, ошибку/повтор, сброс советов и ширину 390 px. Это проверка реального интерфейса без HTTP-сервера, внешнего хостинга и платных вызовов. Первоначальная ошибка пути в новом браузерном тесте исправлена до успешного прогона.

Два отдельно разрешённых вызова реального провайдера: первая контрольная команда завершилась на сравнении русской строки, искажённой PowerShell stdin; итог режима не был зафиксирован. Повторный вызов явно вернул fallback/timeout в пределах 12 секунд. Все обязательные риски, пять вкладов и обе рекомендации сохранились; успешный live-smoke новой версии не заявляется. Ранее live работал; автоматические provider/cache проверки новой версии проходят с подменённым транспортом. Ключ не выводился, в Git не включён. Пользовательские серверы не запускались и не останавливались. Docker/HTTPS текущей версии не проверены LEAD.

Перед отправкой fetch обнаружил новые FRONTEND-коммиты ef6b7db/42c30c6/b7fd6b6/0cdfd75 и merge b10ec88. Получены полноценный dashboard, предварительная валидация, показатели районов, локальное сравнение и карточки альтернатив. Доработка LEAD сначала сохранена отдельным коммитом 4b400c7. При merge вручную объединены main.jsx, scenario.js и styles.css: дизайн/компоненты участника сохранены, обязательные риски не фильтруются, добавлены критерии карточек и «Применить», сохранены порядок формы/проверка актуальности/блокировка двойного клика. В автоматически объединённом package.json обнаружены два ключа test; объединены оба набора unit-тестов одной командой. Доля эффекта в списке мер показывается точно (62.5%, не 63%). Итоговая база проверок: 78 основных + 80 OPS + 32 fallback + 8 frontend unit; production build успешен. Браузерный тест адаптирован к компонентам нового dashboard, обе альтернативы, повтор после ошибки, мобильная ширина и сравнение сохранённого результата перепроверены. Авторство FRONTEND сохранено.
