# Развёртывание — зона OPS

Публичное размещение ещё не выполнено. Добавить сюда конфигурацию согласованной платформы, а рабочий URL и команды — в корневой README.

- Backend: `python -m uvicorn backend.app.main:app --host 0.0.0.0 --port 8000`; health `/api/health`.
- Frontend: `npm ci`, `npm run build`, статические файлы `frontend/dist/`.
- `VITE_API_URL` нужен на этапе сборки frontend и указывает на публичный API.
- `CORS_ORIGINS` у backend содержит точный публичный origin frontend, не `*`.
- Секреты задаются переменными окружения платформы, не включаются в образ или Git.
- Приложению нужны каталоги backend/, ai/, contracts/ и data/, а также requirements.txt.
- Пока нет SQLite и сохранённых состояний; подключение диска не требуется. При их добавлении определить постоянный том и резервное копирование.
- Обеспечить HTTPS, health-check, доступность 24–28.09 и процедуру восстановления.

До живого подключения LLM явно показывать режим mock. Публикация каркаса сама по себе не закрывает обязательный AI-анализ.
