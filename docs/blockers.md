# Запросы и ограничения OPS

Дата: 23.09.2026. После устранения обновить записи и выполнить проверки.

## Актуальный приоритет: README и воспроизводимость

По сообщению пользователя 23.09.2026 организаторы отменили публичный деплой: жюри клонирует репозиторий и запускает проект по README. Публичная доступность больше не блокирует сдачу. Ниже сохранена история предыдущей OPS-задачи; актуальные замечания к main и границы проверки — в [сверке README](readme-review.md).

## FRONTEND-I18N-GRAPHS — замечание интеграции

- После объединения main `de14236` (языки) и FRONTEND `ffbc4c5` (графики) сохранены обе функции, но словарь `frontend/src/translations.js` ещё не содержит новые подписи графиков.
- Воспроизведение: выбрать EN или ҚАЗ; подписи «Профиль показателей», «РАЙОНЫ ГОРОДА», «СИНТЕТИЧЕСКИЕ ДАННЫЕ», «мер в каталоге», «кварталов модели», «СИМУЛЯТОР ГОРОДА» и пояснение пунктирного порога остаются русскими. Наличие этих строк и отсутствие ключей в словаре установлены чтением кода.
- Ожидание: дополнить словарь для обеих локалей, включая aria-label «Параметры модели», затем выполнить существующий браузерный тест на объединённой ветке. Серверные разборы с `data-original-text` по текущему контракту остаются дословно русскими.
- Владелец: FRONTEND. Расчёт, API, общий check и live smoke проходят; OPS не изменял чужой словарь. Это ограничение полноты перевода, а не ошибка модели расчёта.

## OPS-ENV — ЗАКРЫТО

Пользователь явно поручил агенту выполнить установку и Git-операции. Установлены существующие Python-зависимости в `.venv`, Node.js 24.19.0 LTS через winget и frontend-зависимости через `npm ci`; запущен Docker Desktop. Lock-файлы и requirements не менялись. Обязательный check, OPS mock/fallback, сетевые тесты контейнера, smoke и браузерное демо прошли. Результаты: `deployment-status.md`.

Неблокирующее замечание LEAD: Starlette выдаёт deprecation warning для HTTPX в TestClient и рекомендует будущий переход на HTTPX2. Текущий контрактный стек работает; смена зависимостей сейчас не требуется для демо.

## OPS-PUBLISH — ОТМЕНЕНО, историческая запись

- Задача: публичная доступность 24–28.09.
- Требуется: платформа, аккаунт с доступом к командному репозиторию, опубликованный OPS-коммит, фактический HTTPS URL. Railway-плагин найден, подключение пока не подтверждено.
- Ожидание: `python deploy/smoke.py <URL> --require-https --expected-ai-mode mock` проходит; страница работает из другой сети.
- Факт: аккаунт/проект/URL не предоставлены, публикация и внешняя проверка не выполнены. Конфигурация Render/Railway подготовлена.
- Владелец: OPS и владелец аккаунта. Оплата не согласована. Холодный старт Free Render нужно согласовать либо выбрать обеспеченный ресурсами тариф.
- Пока ждём: локальный образ уже проверен; OPS-ветка готовится к интеграции.

## LEAD-LIVE-AI / OPS-API-KEY — ЗАКРЫТО ЛОКАЛЬНО

- Задача: обязательный живой AI-анализ.
- Зона: `ai/agent.py`, `backend/`, зависимости — LEAD.
- Воспроизведение после настройки окружения: backend с `AI_MODE=live`, POST `/api/simulations/analyze` с `contracts/examples/scenario.json`.
- Ожидание финального продукта: реальный LLM-анализ серверных чисел, `analysis.mode=live`.
- Факт: интегрированы main `a78d56e` и FRONTEND `ffbc4c5`, сохранена упаковка `ai/cache/__init__.py`. Пользователь добавил ключ в локальный `.env`; контейнер пересоздан с этим файлом, `AI_MODE=live` и `OPENAI_MODEL=gpt-4.1-mini`.
- Первый smoke рабочего контейнера 23.09.2026 11:49 UTC с `--expected-ai-mode live --require-provider` — PASS: `mode=live`, `source=provider`, `reason=null`, Score 56.54307, две проверенные альтернативы.
- Локальный адрес: `http://localhost:8000`. Секреты не выводились и не включались в Git. Настройка секрета на публичном хостинге остаётся частью OPS-PUBLISH.

## OPS-BRANCH — ЗАКРЫТО

По новому прямому указанию пользователя агент создал `feat/ops`, настроил `team.role=ops` и `core.hooksPath=.githooks`. Авторство пользователя сохранено. Пользователь также разрешил коммит и push OPS-изменений. Ограничение областей редактирования остаётся в силе.

## OPS-INTEGRATION из TASKS — ИСПРАВЛЕНО В feat/ops

Синхронизирован `stable-1` (`fde00b2`). Все три замечания интеграции закрыты в OPS: `ai/cache/__init__.py` включён в образ, in-process тесты изолируют ключ, обёртки не дублируют общий check, текущие описания live обновлены. Новые проверки smoke/Compose/данных, контейнер и браузер прошли. LEAD должен получить последнюю `origin/feat/ops` перед финальным релизом; TASKS меняет только LEAD.

## Применимость требований

Роли employee/admin, персональные записи, изменение задач и свободный текст отсутствуют в SPEC/contracts. Они не добавлены и не объявлены пройденными security-проверками. Расширение требует CONTRACT_REQUEST. Проверка живой LLM на заражённом внутреннем датасете остаётся после её интеграции.
