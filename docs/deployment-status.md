# Фактический статус публикации

## Текущие улучшения OPS, 23.09.2026 10:55–11:03 UTC

- Включён `stable-1` (`fde00b2`). Устранены все замечания OPS-INTEGRATION из TASKS: упаковка cache, изоляция ключа, дублирование тестов, устаревшие описания.
- Общий check — PASS: 66 основных тестов, 124 OPS mock, 32 API fallback, импорт и frontend build.
- `python deploy/container_check.py --build --browser` — PASS: 124 теста против временного контейнера, UID 10001, read-only, `.env`/`.git` отсутствуют, модуль `ai.cache` есть. Runtime image ID `sha256:4f8cc48a999d2998e197931fdd9bf3bb953595d282774fc038174e9ec730a3f9`.
- Браузер — PASS: 52.56 → 56.54 → 55.30 → 56.54, шесть API-запросов на тот же origin, три ответа `mock/template`, ошибок нет.
- Fallback: smoke и 124 HTTP/локальных теста — PASS. Первый совмещённый прогон завершился ошибкой браузерного этапа; отдельный повтор в 11:01 UTC и полный повтор `container_check.py --mode fallback --browser` в 11:02 UTC — PASS, три ответа `fallback/template/missing_api_key`. Причина первого сбоя не установлена; он не засчитан как успешный.
- Обе конфигурации Compose прошли `config --quiet`. Mock Compose фактически запущен на отдельном localhost:18080, smoke — PASS. Тестовый проект удаляется после проверки; рабочий `akim-demo` не изменялся.
- Добавлены безопасная диагностика smoke и сохранение JSON-отчёта, проверка альтернатив/422/закрытых файлов, обязательная доступность API через `--require-api`.
- Настоящий провайдер на этом компьютере и публичная доступность ещё не подтверждены. Нужны ключ в локальном окружении и аккаунт/URL платформы. Эти ограничения не заменяются успешными mock/fallback-проверками.

## Обновление: интеграция live AI, 23.09.2026 10:30 UTC

- В OPS интегрирован `origin/main` до `e5776fb` с сохранением файлов деплоя и авторства коммитов LEAD.
- Обязательная проверка: 66 тестов, импорт приложения и Vite build — PASS. OPS: 80 in-process mock, 32 in-process fallback, 80 против обновлённого контейнера — PASS.
- Исправлена упаковка обязательного `ai/cache/__init__.py`. В Dockerfile добавлена проверка создания приложения от UID 10001 при сборке.
- Docker build — PASS, тег `akim-city:ops`, config ID `sha256:001aca6b0424410896e9cdb1fe60622cd7953e8d43d8b0a82ed2f5a371b750b2`.
- Контейнер `akim-demo` работает на `http://localhost:8000`, запущен с `--env-file .env -e AI_MODE=live`. Создан локальный `.env`, исключённый из Git и образа.
- HTTP smoke — PASS: Score 56.54307, 2 assets, `mode=fallback`, `source=template`, `reason=missing_api_key`. Свежий ответ провайдера пока НЕ подтверждён: нужен ключ владельца и пересоздание контейнера после заполнения `.env`.
- Публичного URL по-прежнему нет. Браузерное демо ниже относится к прежнему mock-образу.

## Предыдущий прогон mock-образа, 23.09.2026 09:54 UTC

| Поле | Значение |
|---|---|
| Дата | 23.09.2026 |
| Ответственный OPS | Акылбек; график 24–28.09 требует подтверждения |
| Платформа | Не согласована; подготовлены Render и Railway |
| Публичный URL | Не получен, публикация не выполнена |
| Версия проверки | OPS-изменения этой ветки на базе `lead: integrate remote history with completed README`; hash OPS-коммита см. в Git |
| AI | Прежний образ: mock, live-адаптер ещё не был интегрирован |
| Docker build | PASS, `akim-city:ops`; ID `sha256:b31390fe7225f5e5214c6600bc72f6e8b63281dbd1e83a0a3966b82c2c8fa573` |
| Обязательный check | PASS: 32 теста, импорт приложения, production-сборка Vite |
| OPS pytest | PASS: 79 in-process mock; 31 in-process fallback; 79 с API контейнера, без пропусков |
| Локальный smoke | PASS, 23.09.2026 09:54 UTC, `http://127.0.0.1:32768`, mock, Score 56.54307, 2 assets |
| Runtime | UID 10001; `.env` и `.git` отсутствуют в `/app`; health-check PASS |
| Внешний smoke / HTTPS / CORS | Не выполнены: нет URL |
| Браузерное демо | PASS, отдельный headless Edge: база 52.56, пример 56.54, школа в Есиле 55.30; 5 API-запросов на тот же origin, ошибок нет |
| Чистый клон | Отдельный чистый клон пока не проверен; Docker собран с чистыми слоями зависимостей |
| Сверка PDF | Выполнена, численных расхождений нет; см. data-review.md |
| Генератор | Выполнен, 5 файлов; одинаковый seed воспроизводится |
| Прямой расчёт | 7 сценариев приняты; PDF 56.54307, школа в Есиле 55.29777; база и пример совпадают с fixtures |
| Синтаксис | Python AST: 8 файлов, PowerShell parser: check.ps1, без ошибок |
| Git whitespace | `git diff --check` прошёл для отслеживаемых изменений |

После публикации добавить UTC-время, URL, hash коммита, версию датасета, режим AI и фактические результаты. Не считать SKIP или наличие Dockerfile доказательством релиза. Обновить URL также в README.

Локальная среда: Windows, Python 3.13.14, Node.js 24.19.0, Docker Engine 29.6.1. Python-зависимости установлены в `.venv`, frontend через `npm ci`, lock-файлы не изменены. Тестовый контейнер и временный браузер остановлены после конечного прогона. Локальный адрес выше больше не обслуживается.

Предупреждение зависимости: Starlette сообщает о будущей замене HTTPX в TestClient на HTTPX2. Текущие проверки проходят; список зависимостей оставлен без изменений, вопрос передан LEAD в blockers.md.
