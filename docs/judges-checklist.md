# Приёмка демо

Статус 23.09.2026: локальное браузерное демо в headless Edge успешно (52.56 → 56.54 → 55.30); API и assets обращаются на один origin без ошибок. Публичная доступность и ручная приёмка человеком ещё не подтверждены. Флажки ниже предназначены для финальной проверки публичного релиза.

Чистый клон `9447065` проверен агентом в 11:04 UTC: Compose, 124 OPS-теста и браузерное демо прошли. Это закрывает локальную воспроизводимость; публичная приёмка остаётся отдельной.

- [ ] В README указан настоящий URL; он открывается через HTTPS с мобильного интернета.
- [ ] При первом открытии видны бюджет 100, база 52.56, пять районов; данные синтетические.
- [ ] Предзаполненный пример: M7/M8/M10 Нура, M12 город, M5 Сарыарка.
- [ ] «Рассчитать сценарий»: стоимость 95, остаток 5, Score 56.54, прирост +3.99, критических показателей 0.
- [ ] Перенос M7 в Есиль меняет Score. Повтор исходного примера возвращает прежний результат.
- [ ] Перерасход блокируется UI; прямой запрос дороже 100 возвращает 422 без Score.
- [ ] Дубли, M1/M3 и M4/M7 в одном районе возвращают понятные причины.
- [ ] Видно notice и настоящий режим AI. Mock/fallback не представлены как live.
- [ ] Для финальной защиты отдельно подтверждён настоящий LLM-анализ.
- [ ] DevTools: API на публичном origin, нет localhost, CORS/mixed-content ошибок, assets загружаются.
- [ ] Локальная остановка backend человеком даёт понятную ошибку UI; после запуска и обновления страница восстанавливается.
- [ ] Общий check (включая OPS), контейнерная проверка и внешний smoke прошли; удалённый pytest запускался с `--require-api`, без пропусков.
- [ ] Первый анализ после запуска подтвердил `live/provider`; кэш не выдан за свежий вызов модели.
- [ ] Один рекомендованный набор повторно рассчитан: его Score и бюджет совпали с alternative, исходный сценарий автоматически не менялся.
- [ ] После проверки в чистом клоне записаны ОС, версии Python/Node/Docker, commit и выполненные команды.
- [ ] Ответственный и резервный контакт знают процедуру восстановления; ресурсы на 24–28.09 подтверждены.

Для запуска с исходниками инструкции в README; контейнер — `deploy/README.md`. Прогон фиксировать в `deployment-status.md`, результаты security — в `security-report.md`.
